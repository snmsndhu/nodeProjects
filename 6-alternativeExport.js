// here we can use different methods to export the modules
// one is ==>
module.exports.items = ["item1", "item2"];

// second would be ==>
const person = {
  name: "bob",
};

module.exports.singleName = person;

// these are the two different syntax to export the modules
